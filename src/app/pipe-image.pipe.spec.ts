import { PipeImagePipe } from './pipe-image.pipe';

describe('PipeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
